# dependencies
node_modules/
.pnp/
.pnp.js

# builds
.next/
out/
dist/
build/
next-env.d.ts
*.tsbuildinfo

# env
.env
.env.local
.env*.local

# logs
npm-debug.log*
yarn-debug.log*
*.log

# OS
.DS_Store
Thumbs.db

# NOTE: the generated seed dataset (data/seed/*.json) IS committed so the deployed
# Catalyst app has data at runtime. Regenerate any time with `npm run data:generate`.

# Catalyst import CSVs are regenerable from the seed — don't commit
data/csv/

# The official KSP ERD is marked "Confidential" — reference only, never redistribute publicly
docs/*.pdf

# catalyst
.catalyst/
catalyst/**/node_modules/
# generated deploy artifacts (machine-specific paths; regenerated by catalyst/scripts/build-appsail.mjs)
.catalystrc
/catalyst.json

# editor
.vscode/*
!.vscode/extensions.json
.idea/
